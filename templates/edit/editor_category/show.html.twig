{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('editor_category_index') }}">Retour à la liste</a>

    <a href="{{ path('editor_category_edit', {'id': category.id}) }}">Editer</a>

    {{ include('edit/editor_category/_delete_form.html.twig') }}
{% endblock %}
