
{{ form_start(form) }}

        <div class="form-floating mb-3">

            {{ form_widget(form.name, {'attr': {'class': 'form-control search'}}) }}
            {{ form_label(form.name, 'Nom', {'label_attr': {'class': 'form-label'}}) }}
    
        </div>

        <div class="form-floating mb-3">

            {{ form_widget(form.parent, {'attr': {'class': 'form-control form-select search'}}) }}
            {{ form_label(form.parent, 'Parent', {'label_attr': {'class': 'form-label'}}) }}
    
        </div>
    
        <div class="d-flex justify-content-center m-3">
    
            <button class="btn btn-new mt-4">{{ button_label|default('Enregistrer') }}</button>
            
        </div>

        {{ form_end(form) }}
