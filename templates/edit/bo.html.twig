{% extends 'base.html.twig' %}

{% block title %}Gestion du back office{% endblock %}


{% block body %}

<div class="container py-3  d-flex justify-content-center">
    <div class="card card-login m-5">
      <div class="card-body p-5 m-3">

          <div class="md-5 mt-md-4 pb-3">

              <h2 class="fw-bold mb-2 title">Gestion du back office</h2>

          </div>

          <p class="mb-5">

            <div class="example-wrapper">
            
                <table class="table">
                    <tbody>
                      <tr>
                        <th>Gestion des médias</th>
                        <td><a href="{{ path('edit_media_index')}}" class="btn">Voir</a></td>

                      </tr>

                      <tr>
                        <th>Gestion des catégories</th>
                        <td><a href="{{ path('editor_category_index')}}" class="btn">Voir</a></td>

                      </tr>

                      <tr>
                        <th>Gestion des utilisateurs</th>
                        <td><a href="{{ path('editor_user_index')}}" class="btn">Voir</a></td>

                      </tr>
                    </tbody>
                </table>

            </div>

        </p>
</div>
</div>
</div>
</div>

{% endblock %}
