
{{ form_start(form) }}

        <div class="form-floating mb-3">

            {{ form_widget(form.email, {'attr': {'class': 'form-control search'}}) }}
            {{ form_label(form.email, 'E-Mail', {'label_attr': {'class': 'form-label'}}) }}
    
        </div>

        <div class="form-floating mb-3">

            {{ form_widget(form.roles, {'attr': {'class': 'form-control search alt-label'}}) }}
            {{ form_label(form.roles, 'Roles', {'label_attr': {'class': 'form-label'}}) }}
    
        </div>
    
    
            <button class="btn btn-new mt-4 float-end">{{ button_label|default('Enregistrer') }}</button>
    

        {{ form_end(form) }}

        