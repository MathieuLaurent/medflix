{% extends 'base.html.twig' %}


{% block title %}{{ user.name }} {{ user.firstname }}{% endblock %}


{% block body %}

<div class="container py-3  d-flex justify-content-center">
    <div class="card card-login m-5">
      <div class="card-body p-5 m-3">

          <div class="md-5 mt-md-4 pb-3">

              <h2 class="fw-bold mb-2 title">{{ user.name }} {{ user.firstname }}</h2>

          </div>

          <p class="mb-1">

            <div class="example-wrapper">
            
                <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>

            <tr>
                <th>Nom</th>
                <td>{{ user.name }}</td>
            </tr>

            <tr>
                <th>Prénom</th>
                <td>{{ user.firstname }}</td>
            </tr>

        </tbody>
    </table>

    <div class="d-flex justify-content-center m-3">

        <a href="{{ path('editor_user_index') }}" class="btn mx-3">Retour à la liste</a>

        {{ include('edit/editor_user/_delete_form.html.twig') }}
    </div>

        </div>

        </p>

        </div>
        </div>
    </div>

{% endblock %}