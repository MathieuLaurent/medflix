 <nav class="navbar navbar-expand-sm">
      <a class="navbar-brand" href="{{path("accueil")}}">
        <img src="img/logo.png" alt="" class="logo">
      </a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExample03" aria-controls="navbarsExample03" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse d-flex justify-content-end align-items-center" id="navbarsExample03">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item dropdown">
            <a class="nav-link px-4 fs-5 dropdown-toggle" href="#" id="dropdown03" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Explorer</a>
            <div class="dropdown-menu" aria-labelledby="dropdown03">
              <a class="dropdown-item" href="#">pdf</a>
              <hr>
              <a class="dropdown-item" href="#">Images</a>
            </div>
          </li>

          <li class="nav-item">
            <a class="nav-link px-4 fs-5" href="#">Media</a>
          </li>

          <li class="nav-item">
            <a class="nav-link px-4 fs-5" href="#">Categories</a>
          </li>
         
        </ul>
      </div>
       <ul class="navbar-nav">
            {% if not app.user %}
                <li class="nav-item"><a class="nav-link px-4 fs-5 link-light fw-bold nav-user" href="{{path('app_register')}}">Inscription</a></li>
                <li class="nav-item"><a class="nav-link px-4 fs-5 link-light fw-bold nav-user" href="{{path('app_login')}}">Connexion</a></li>
            {% else %}
                    <li class="nav-item dropdown">
                    <a class="nav-link px-4 fs-5 link-light fw-bold dropdown-toggle nav-id" id="navbarDropdown" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">{{app.user.firstname}} {{app.user.name}}</a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="#">Profil</a></li>
                        {% if is_granted('ROLE_WRITTER') %}
                            <li class="nav-item"><a class="dropdown-item" href="{{path('editor')}}">Administration</a></li>     
                        {% endif %}
                        <li><hr class="dropdown-divider" /></li>
                        <li class="nav-item"><a class="dropdown-item" href="{{path('app_logout')}}">Déconnexion</a></li>
                    </ul>
                </li>
                
                
            {% endif %}
        </ul>
    </nav>

    <header class="py-5 header d-flex justify-content-center align-items-center">
      <div class="container-custom">
        <p class="lead m-auto fw-normal mb-3 display-4">Les plus belles photos de voyages pour une évasion assurée.</p>
        {{render(controller('App\\Controller\\SearchNavController::renderSearchNav'))}}
      </div>
    </header>

        

    </nav>
